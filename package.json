{
  "name": "@antennajs/monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.29.4",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
